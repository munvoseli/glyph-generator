This is supposed to help make glyphs for conlangs.

In one iteration, it changes (at random) a few (10) pixels in the set of glyphs.  If that makes the glyphs more distinct (as measured by blurring-or-such the pixels), then the change is kept.  The change is otherwise lost.
